apiVersion: v1
kind: Service
metadata:
  name: {{ include "tfrs-celery.fullname" . }}
  labels:
    {{- include "tfrs-celery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tfrs-celery.selectorLabels" . | nindent 4 }}
